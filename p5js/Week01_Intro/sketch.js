//Setup runs through your code once.
//The computer reads your code from top to bottom.
function setup() {
  //Dimensions of your canvas.
  createCanvas(400, 300);
  //Background color of your canvas .
  //Color ranges from 0 (black) to 255 (white).
  background(200);
  fill(50);
  rect(0, 0, 100, 100);
  //Printing out information to the javascript console below.
  print("Hello world!!!");
}